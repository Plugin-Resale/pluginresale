# Plugin Resale — project brief

Marketplace for second-hand audio plugin licenses (competitor to Knobcloud).
Domain: pluginresale.com (Namecheap). Contact: contact@pluginresale.com.
Site language: **English**. Owner: Victor (non-developer, explain steps simply, in French).

Design reference (mockup v1, 4 pages): https://claude.ai/artifact/KiyRncyZgYt9eo3hP2ENFw

## Business rules
- Free to list, free to buy, no commission. Revenue = ads only (for now).
- **Payment happens directly between users via PayPal Goods & Services.** The site never holds money, never processes payments, never handles disputes or refunds (PayPal does).
- Each developer has its own license-transfer policy → a public "transfer rules" database is a core feature.

## Stack
- Next.js (App Router, TypeScript), deployed on Vercel. Server functions pinned to `dub1` (Dublin) in `vercel.json`, next to the Supabase database (`eu-west-1`, Ireland): they used to run in `iad1` (Washington), which put every query across the Atlantic (pages ~2.5x slower, occasional timeouts). Keep them in the same region as the database.
- Supabase: Postgres, Auth (email magic link), Row Level Security on every table.
- No payment SDK.

## Pages (match the mockup)
1. **Home** — hero + search, category chips, "Just listed" grid, how it works, developers teaser, footer.
2. **Browse** — filters (category, developer, price, transferable only, no dev fee), sort, paginated grid.
3. **Listing** — plugin, formats, seller note, developer transfer rules box, price card, "Buy with PayPal", "Message seller", seller card + rating.
4. **Sell** — form: plugin (autocomplete from DB), version, category, price, description, PayPal email, 2 mandatory checkboxes (owns license / not NFR-edu-bundled; will uninstall and transfer). Side panel auto-fills the developer's transfer rules.
5. Also: Developers index + developer page (transfer rules), seller profile with reviews, account (my listings, my purchases), sign in, Terms, Privacy, Advertise.

## Purchase flow
1. Buyer clicks "Buy with PayPal" (must be signed in) → a `deal` is created, listing becomes "reserved".
2. Buyer sees the seller's PayPal email + instructions: pay with **Goods & Services** (never Friends & Family), add the listing ID in the note.
3. Seller marks "payment received" → seller starts the developer transfer.
4. Buyer marks "license received" → deal completed, listing "sold".
5. Both leave a review (1–5 + comment). Seller's PayPal email is never public, shown only to the buyer of an active deal.
6. Deal can be cancelled by either side before step 3 → listing back to "active".

## Data model (first draft)
- `profiles` (id = auth user, username, created_at)
- `developers` (id, name, slug, transferable bool — **null = "policy not verified"** (listing allowed, orange badge + warning asking both sides to check with the developer), fee text, who_pays text, process text, typical_delay text, source_url, last_verified date)
- `plugins` (id, developer_id, name, slug, category)
- `listings` (id, seller_id, plugin_id, version, price_eur, description, paypal_email [private], status active|reserved|sold|removed, created_at)
- `deals` (id, listing_id, buyer_id, seller_id, status requested|paid|completed|cancelled, timestamps)
- `reviews` (id, deal_id, author_id, target_id, rating, comment)
- `messages` (id, listing_id, from_id, to_id, body, created_at)

## Catalogue maintenance rules
- **Major versions of the same product are separate catalogue entries by default** (Victor, 2026-09-24, after FabFilter Pro-Q 3 got wrongly merged into Pro-Q 4). Never rename or merge an older major version (e.g. "Pro-Q 3" → "Pro-Q 4", "Altiverb 7" → "Altiverb 8") into the current one unless the developer's own site explicitly confirms the upgrade between them is free. Most developers charge for major-version upgrades (FabFilter, Altiverb, Pianoteq confirmed so far), which makes the old and new versions different products with different resale value — merging them makes the older one unlistable. When unconfirmed either way, default to keeping them separate: extra catalogue rows are harmless, a wrongful merge silently breaks resale for whoever owns the older version. u-he is a confirmed exception (free updates for life), which is why Zebra Legacy and Zebra 3 are correctly two separate rows.
- **The catalogue is not meant to be exhaustively pre-curated any more** (Victor, 2026-09-24, after the Mantra/Ultramarine/FabFilter episodes made clear that guessing every developer's whole back-catalogue in advance does not scale): since `0035_seller_submitted_plugins.sql`, a seller whose plugin is not in the list can add it themselves from the Sell form — developer picked from the existing (policy-verified) list, plugin name and category typed freely, `plugins.created_by` records who added it. This is now the primary way the catalogue should grow; a big manual audit pass is the exception, not the default, going forward.

## Ads
- One `<AdSlot>` component with a placeholder, positions from the mockup:
  A 970×90 leaderboard under header (all pages) · B native card in grids (every 6th) · C 970×250 billboard home · D 300×600 sticky rail on Browse · E 300×250 on Listing.
- No ads on the Sell page. Ad network (AdSense or direct) plugged in later.

## Design tokens
- Colors: ground #F2EFE8, surface #FFFFFF, ink #17171B, muted #55545C, line #D9D4C9, accent #C2410C (hover #9A3412), success #1F6B44 on #E1F0E6.
- Fonts (Google): Bricolage Grotesque (display), IBM Plex Sans (body), IBM Plex Mono (prices, labels).
- Radius 8–14px, buttons 44px min height.

## Legal (EU)
- Footer disclaimer: payments are between users, Plugin Resale is not a party to the transaction.
- Terms, Privacy (GDPR), cookie consent before loading ad scripts.
- Report-listing button on every listing.

## Build order
1. Project setup + Supabase + deploy empty site on pluginresale.com.
2. Auth + profiles.
3. Developers/plugins tables + seed data + developer pages.
4. Sell form + listings + Browse + Listing page.
5. Deals flow + reviews + messages. **5a done** (deals table + `/deals/[id]` + My sales/purchases, tested end-to-end with 2 accounts: victor.malvolti@gmail.com as seller, contact@pluginresale.com as buyer). **5b done** (messages table + `send_message` RPC + `/listings/[id]/messages` thread + seller inbox per listing + Messages section on the account page; tested end-to-end). **5c done** (reviews table + `add_review` RPC, one review per side of a completed deal; `seller_stats` view; `seller_avg_rating`/`seller_review_count` on listing cards and the listing page seller card; public profile `/u/[username]` with completed sales, average rating, and the review list; tested end-to-end both directions). Step 5 (deals + reviews + messages) is now fully done.
6. Ad slots, legal pages, polish, launch. **Ads deprioritized** (Victor 2026-09-23: focus on getting users before launch, skip ad slots/network/cookie-consent for now — revisit later). **Custom SMTP done** (Resend, connected in Supabase Auth SMTP settings; magic-link.html template live for Magic Link + Confirm signup). **Email notifications done** (new buyer, payment confirmed, license received, new message — sent via the Resend HTTP API from `src/lib/email.ts`; recipient's email looked up with a service-role client in `src/lib/supabase/admin.ts`, since `SUPABASE_SERVICE_ROLE_KEY` is needed to read another user's email; tested end-to-end for all 4). **Legal pages done** (`/terms`, `/privacy`, `/legal` = mentions légales LCEN, all linked from the footer; operator SAS Boring Vic, capital €300, VAT FR11978440972). **Legal compliance pass done 2026-09-24** (migration `0009_legal.sql`): Terms acceptance checkbox on the username form (`accept_terms()` RPC, `profiles.terms_accepted_at`, a trigger refuses a username without it; Sell page gated on it); private-individual sellers only (3rd mandatory checkbox on Sell + "Private seller" notice on the listing page); `/report` form (DSA notice-and-action: emails contact@ with reply-to + sends the reporter a receipt); self-service account deletion on My account (`delete_my_account()` anonymises the profile to `deleted_xxx`, removes listings/PayPal emails, archives email+username in the service-role-only `deleted_accounts` table for 1 year as LCEN requires, then the app deletes the auth user or, if FKs block it, scrubs its email to `@deleted.pluginresale.com` and bans it); tax reminder (CGI art. 242 bis) on the seller's deal page. **Catalogue extension done 2026-09-24** (migration `0011_catalogue_extension.sql`, generated by a script from researched data): 121 developers (77 transferable, 30 not, 14 not verified), ~1,180 plugins. Transfer rules come only from official developer pages (or iLok for iLok-only policies), each with `source_url` + `last_verified`; forum-only info → not verified. Full official catalogues for FabFilter, Waves (from waves.com/llm/products.json), Acustica (store, categorised with Acustica's own shop filters), Pulsar Audio, Pulsar Modular, Newfangled, Tone Projects, Schwabe Digital; other developers have their 5–15 flagship products. Categories are now 12: added `channel-strips`, `guitar-amps`, `microphones`. **DAW category added 2026-09-24** (migration `0012_daw_category.sql`): new `daw` category (13 total), with the 5 biggest DAW publishers — Ableton (Live 12 Suite/Standard/Intro, free self-service transfer), Image-Line (FL Studio Producer/Signature/All Plugins Edition, EULA forbids resale — `transferable = false`), Steinberg (Cubase Pro/Artist 15, Nuendo 15, Dorico Pro 6 added to its existing developer row), Avid (Pro Tools Studio/Ultimate perpetual licenses, Transfer of Ownership program), Fender (Studio One was rebranded Fender Studio Pro in Jan 2026 — new developer row, transfer policy not yet published for the new product, `transferable = null`). Logic Pro excluded (App Store only, non-transferable). 125 developers total. **50 more developers + Native Instruments completed 2026-09-25** (migrations `0013_50_more_developers.sql`, `0014_native_instruments_catalogue.sql`): Waves catalogue reconfirmed complete (300 products, no re-import needed). 50 new developers with 2–4 flagship products each, strong VSTi/synth coverage (Vengeance Sound, Synapse Audio, Tone2, Sonic Charge, Vital Audio, Roland Cloud, Reason Studios rack extensions, Xhun Audio, Sinevibes, Wusik, etc.) alongside guitar amp sims (Two Notes, Nembrini, Mercuriall, Audio Assault), mastering/mixing utilities (Cytomic, TBProAudio, DDMF, HOFA, Flux, Metric Halo...) and sample libraries (Sample Logic, Aaron Venture, Soniccouture, Vir2, etc.) — 175 developers total, 1383 plugins total. Native Instruments catalogue completed from 18 to 65 products: outdated versions corrected (Komplete 26, Absynth 6), plus Kontakt/Reaktor synths, effects, Play Series instruments, Traktor Pro 4, Maschine 3. **Gap-fill pass done 2026-09-25** (migration `0015_gap_fill.sql`): added 3 developers that were missing (Serato, KV331 Audio, Synchro Arts); deepened Spitfire Audio, Vienna Symphonic Library, UVI, ProjectSAM and Toontrack, whose catalogues were too thin for their real size; corrected Output's policy after re-reading its EULA — transferable, but only when both buyer and seller are based in the EU or UK. 178 developers total, 1404 plugins total. **Deep-catalogue pass done 2026-09-26** (migrations `0016_deepen_30_developers.sql`, `0017_deepen_70_more_developers.sql`): ~96 developers brought to a complete or near-complete range — 30 with bounded, well-known catalogues fully covered (iZotope, Arturia, u-he, Soundtoys, Valhalla DSP, Xfer Records, Kilohearts, Sonnox, DMG Audio, D16 Group, Slate Digital, McDSP, Softube, MeldaProduction, Boz Digital Labs, Klanghelm, Voxengo, TAL Software, GForce Software, Cherry Audio, Sugar Bytes, Applied Acoustics Systems, Modartt, Synthogy, Spectrasonics, reFX, LennarDigital, Sonic Academy, Klevgrand, Goodhertz), 66 more with a representative top slice (Celemony, Antares, Waldorf, Eventide, Universal Audio, Plugin Alliance, Neural DSP, IK Multimedia, plus most of the sample-library/synth roster). A few outdated entries were fixed along the way (iZotope RX 11→12, Ozone 11→12, a duplicate Neutron 4, three mis-named Arturia instruments). 8 developers had fabricated product names caught and corrected during self-review before pushing (Karanyi Sounds, Fluffy Audio, Red Room Audio, VI Labs, United Plugins, Three-Body Technology, Polyverse Music, Minimal Audio) — the ~60 smaller/less-known developers in this pass were not individually re-verified one by one, so a residual risk of an inaccurate product name remains there; flag and fix if spotted. 178 developers total, 1634 plugins total. **Sitemap-sourced deepening + cleanup pass done 2026-09-26** (migrations `0018_deepen_pa_uad_melda.sql`, `0019_fix_incomplete_developer_records.sql`): ~570 more products for Plugin Alliance, Universal Audio and MeldaProduction, whose real catalogues run into the hundreds — names pulled directly from each developer's own store sitemap (not memory) to keep the earlier accuracy problem from recurring; category assignment was keyword-heuristic and needed real cleanup (~109 products, mostly guitar amps/synths/channel strips that had defaulted to `utilities`, re-categorized by hand in `0019`). Audit also caught and fixed: 2 developers with a completely empty policy (Neural DSP, Three-Body Technology — no official page found, `transferable = null`), 1 missing source citation (Tokyo Dawn Records), and 1 duplicate plugin (GetGood Drums "Modern & Massive" under two slugs). 178 developers total, 2205 plugins total. **DAW category expanded 2026-09-26** (migrations `0020_more_daws.sql`, `0021_even_more_daws.sql`): 13 more DAW makers added on top of the original 5 — REAPER, Bitwig, MOTU (Digital Performer), Apple (Logic Pro — now included after all, non-transferable but still listed for the info), Adobe (Audition, subscription-only, no product listed), Magix (Samplitude/Sequoia), Harrison Audio (Mixbus), Renoise, n-Track, Acoustica (Mixcraft), Cakewalk (BandLab membership, no product listed), Ardour (free/GPL, no product listed). Reason Studios and Tracktion corrected: their own DAWs are transferable in practice even though Reason's Rack Extension add-ons aren't and Tracktion's written terms say otherwise. 28 DAW-category products across 18 publishers now (was 13 across 5). 190 developers total, 2220 plugins total. **Roughly doubled 2026-09-26** (migrations `0022_double_the_catalogue.sql`, `0023_more_daw_editions.sql`): ~2,100 more products for Spitfire Audio, 8dio, Toontrack and Native Instruments (sitemap-sourced, same technique as step 18) — 2220 → ~4324 plugins. DAW category widened further (Cubase Elements 15, Dorico Elements 6, Samplitude Pro X8 Suite, Mixbus32C, two n-Track tiers, Mixcraft Pro Studio, Bremmers Audio Design/MultitrackStudio); Serato Studio reclassified from `utilities` to `daw`; Samplitude Pro X and Mixcraft renamed to their current versions (X8, 10.6) — 28 → 37 DAW products. No realistic way to double DAW count specifically: there just aren't that many genuinely distinct commercial DAWs left uncovered. 191 developers total, 4324 plugins total. **Catalogue audit 2026-09-24** (migrations `0024_audit_fixes.sql`, `0025_audit_fixes_sitemap_catalogues.sql`, `0026_audit_fixes_native_instruments.sql`, paste in that order): every change checked against the developer's own site/store/EULA, source on each SQL line. 0024 = smaller developers: ~85 product names that don't exist at that developer (fabricated, or another company's product, e.g. Aaron Venture, Auddict, Fracture Sounds, Rast Sound, Cinematique, Denise, Wave Alchemy, Precisionsound had no real product at all) deleted and replaced by 51 real ones from the official catalogues; free plugins removed (OTT, Supermassive, TAL-Chorus-LX, IVGI, D16 Frontier...); hardware removed (Slate ML-1/ML-2, Positive Grid Spark); outdated versions renamed (Altiverb 8, Pianoteq 9, Heat Up 3, Music Production Suite 9, FX Collection 6, SpectraLayers Pro 13, Zebra Legacy/Zebra 3...); old-version duplicates merged into the current product (the version a seller owns goes in the listing's `version` field); Adobe/Audio Modeling policy sources fixed; `last_verified` cleared on unverified (`transferable = null`) policies. 0025 = sitemap catalogues re-checked against the same stores' product feeds: 8dio/Spitfire official names and clean slugs, free items removed, Plugin Alliance "marketplace" items moved to their real developers (Baby Audio, Krotos, GForce, Mastering The Mix, Fuse Audio Labs), PA categories fixed from PA's descriptions, Melda's free MFreeFXBundle plugins removed, Toontrack/UAD spelling (EZkeys, EZmix, UAD). 0026 = Native Instruments against the official NI store (vendor/price/type per product): ~600 of the 0022 "NI" products were third-party NKS partner products; moved to their real developer when it exists in the catalogue, merged when already listed there, removed otherwise (277, re-add them under their own developer once its policy is checked); NI hardware/accessories and free products removed; names/categories aligned with the store. Net: 191 developers (unchanged; Zero-G now has no product left), 4324 -> 3717 plugins. A product is never deleted while a listing points at it (merges move the listing first). Residual, not verifiable from official pages (left as is): Chris Hein Instruments names, STL Tonality Andy James, Karanyi Continuo/Polyscape/Vapor Keys 2, Fluffy Audio (catalogue may now be free), Red Room Audio (now Impact Soundworks), Soundiron Emotional Piano/Hyperion Strings, Big Fish Drums Overkill, Samplemodeling The Sax Brothers, Orange Tree Evolution Nylon String, UJAM Finisher Micro/Usynth Core, Minimal Audio (site blocked), Steinberg HALion 7/Groove Agent 5/Padshop 2/Retrologue 2 versions, Antares Auto-Tune Artist/Access (no longer listed) and Mic Mod/Throat (now subscription-only), iZotope Iris 2, Exponential Audio R4/Nimbus (discontinued 2022), NI Knifonium/Passive EQ/Enhanced EQ (not on the NI store), BFD Player, Dreamtonics Synthesizer V Studio Basic. **Blog started 2026-09-24**: `/blog` index + `/blog/[slug]` articles, linked from header and footer. Articles are Markdown files in `content/blog/<slug>.md` (front matter: title, description, date YYYY-MM-DD, optional updated; a `_` prefix = draft, not published), rendered with `marked` by `src/lib/blog.ts`; publishing an article = commit + push, no database. Each article has OpenGraph + BlogPosting JSON-LD and a Sell/Browse call-to-action. Articles are in English. First article: "Can I legally resell my audio plugins?" (UsedSoft v Oracle for the EU, Vernor v. Autodesk for the US, licenses usually excluded, how to resell properly). **10 more articles 2026-09-24**: general transfer guide, buying safely, pricing a used plugin, Waves / Native Instruments / iZotope transfer guides, NFR-EDU-bundle-upgrade licenses, iLok transfers, selling a DAW license, and a data study of the 191 developers' policies (figures computed from the migrations replayed into a local Postgres; refresh them if the database changes a lot). Developer facts come from the `developers` table plus the official pages it cites; don't add policy details from memory. **Scheduled publishing** (Victor, 2026-09-24): a post dated in the future stays hidden (404, not in the index) until that day, Paris time, and appears on its own without a redeploy; links to not-yet-published posts render as plain text. Only 3 articles were kept live on 24 Sep (legal resale, DAW, pricing); the other 8 go out every 2–3 days: transfer guide 26 Sep, buying safely 29 Sep, 191-developer study 1 Oct, Waves 4 Oct, NI 6 Oct, iLok 9 Oct, iZotope 11 Oct, NFR/EDU/bundles 14 Oct. Next: polish, then launch (Advertise page still not built, deferred with ads). **Acustica Audio completeness pass 2026-09-24** (migration `0028_acustica_completeness.sql`, sitemap-sourced like 0018/0025): renamed `Aquamarine5` to `Aquamarine 5` (its URL slug had dropped the space, not its real name) and added 5 real products that had their own store page but were missing — NebulaMan2, Squid3 - Electro Edge, Squid3 - Analog Life 1, Blackout for SQUID-3, Expanse5 - Zygote. Everything else in the ~220-URL Acustica sitemap matched an existing catalogue row once abbreviated URL slugs (e.g. `SIENNACOMB` for "Sienna Volume B", `BAXEQ` for "Dangerous Music Bax Eq") were accounted for; a handful of Nebula-era items (NEB4SERVER, N4PLAYER, M4PLAYER, M4CHAN) were left out, their product pages were ambiguous/redirected and not worth guessing at. 191 developers total (unchanged), 3722 plugins total. **Acustica name fixes 2026-09-24** (migration `0029_acustica_name_fixes.sql`, Victor spotted Ultramarine missing right after 0028): same squished-slug issue as Aquamarine5 on 8 more products — Ultramarine4, Pink4, Jade2, Cola2, Honey3, Copper2, Mantis2, Bronze2 renamed to their real "Name N" form (with the space), Alice9 renamed to "Alice-9" (hyphen, not a space). Also removed TH2, a free product. 3721 plugins total (net -1 after the TH2 removal). Checked every remaining Acustica slug for the same "digit glued to a word" pattern; Squid3, Expanse5, NebulaMan2, TH2 (name), Fire The LP8 and Ch3rry Equalizer are the developer own stylised names, left as is. **PayPal email remembered 2026-09-24** (migration `0027_remember_paypal_email.sql`): new private `profile_private` table (RLS: owner only, same pattern as `listing_private`) storing the seller last-used PayPal email; `create_listing()` now saves whatever email it is given there, and the Sell page prefills the PayPal field from it — sellers only type it once. **Fixed 1,000-row cap on plugin lists 2026-09-24** (code, no migration; Victor confirmed Ultramarine 4 was in the database and on its developer page but missing from the Sell search): Supabase caps a plain `select()` at 1,000 rows, and the catalogue is well past that now — `src/app/sell/page.tsx` (all plugins, sorted by plugin name across every developer) and `src/app/developers/[slug]/page.tsx` (a single developer's plugins, for developers like Native Instruments/Toontrack/8dio that individually have 1,000+ products) now page through with `.range()` until a batch comes back short. Worth remembering for any future query on `plugins` or another large table. **Neural DSP Mantra added 2026-09-24** (migration `0030_neural_dsp_mantra.sql`, Victor asked for just this one product): an all-in-one vocal production plugin, category `utilities` like the catalogue other vocal suites (Nectar, CLA Vocals...). Neural DSP transfer policy is still unverified (`transferable = null`), unchanged. **Neural DSP catalogue completed 2026-09-24** (migration `0031_neural_dsp_catalogue.sql`, Victor asked to complete it): checked the full current lineup via neuraldsp.com/sitemap.xml (23 product URLs) and added the 11 that were missing — Archetype: John Mayer X, Misha Mansoor X, Rabea X, Mateus Asato, Tom Morello, Darkglass Ultimate, Morgan Amps Suite, Mesa Boogie Mark IIC+ Suite, Tone King Imperial MKII, Fortin Cali Suite, Omega Ampworks Granophyre. 23 products total now, all Neural DSP own current sitemap. No dedicated bass category, so Darkglass Ultimate went under `guitar-amps` like the existing Parallax X. **Found and fixed a real conversion-funnel bug 2026-09-24** (code, no migration; Victor noticed 46 visits but 0 external listings, only 3 signed-up users who all set a username + accepted Terms but have no listing, deal or message): the "Message seller" flow failed silently — `sendMessage` always redirected with a generic `error=1` that `/listings/[id]/messages` never read or displayed, so a buyer without a username who tried to message a seller saw their message just vanish with no explanation. Fixed: `sendMessage` now maps the RPC's error (`USERNAME_REQUIRED`, `EMPTY_BODY`, etc.) to a specific code and the messages page shows it, same pattern the Buy button already had. Also added a `next` redirect: My account's username form now takes a `?next=` (from Sell, a listing's Buy error, or a listing's Message error) and sends the user straight back to what they were doing once they save their username, instead of stranding them on My account. Root cause of "0 listings" was very likely this: real visitors got as far as trying to buy or message, hit the username requirement, and had no easy way back. **FabFilter Pro-Q 3 / Pro-C 2 restored 2026-09-24** (migration `0032_fabfilter_paid_upgrades.sql`, Victor noticed Pro-Q 4 in the catalogue but no way to list an older Pro-Q): 0024 had merged them into Pro-Q 4 / Pro-C 3 under the "keep only the current version, exact version goes in the listing" rule — wrong for FabFilter, which explicitly charges for major-version upgrades (fabfilter.com/support/faq: "a new major version is a brand new plug-in"), so Pro-Q 3 and Pro-Q 4 are different products with different resale value. **Strategy decided and applied 2026-09-24** (migration `0033_more_paid_upgrade_restores.sql`; see Catalogue maintenance rules): checked the other 5 flagged developers — Altiverb and Pianoteq explicitly confirmed paid upgrades on their own site (Pianoteq: $29 fee), iZotope Music Production Suite and Arturia FX Collection show a priced crossgrade/upgrade offer rather than a free one, Zebra (u-he) is a confirmed free-upgrade exception and was already correctly split. Restored as separate catalogue entries: Altiverb 7 XL, Altiverb 7 Regular, Music Production Suite 7, FX Collection 5, Pianoteq 8 Pro/Standard/Stage, SpectraLayers Pro 11 (Steinberg does not publish free-upgrade language for SpectraLayers either, treated the same way by default per the new rule). **Full-catalogue sweep for the same mistake 2026-09-24** (migration `0034_full_catalogue_version_audit.sql`, Victor asked to check everywhere): scanned all 386 renames across every past migration for a version number changing inside a rename (the shape of the FabFilter/Altiverb/Pianoteq mistakes) — 21 candidates, 17 were false positives (8dio add-on slug cleanups, or fixing a fabricated version number like Kazrog KClip 4 that never existed), 4 were real: Audio Ease Speakerphone 2→3 (confirmed paid, same upgrade program as Altiverb), Initial Audio Heat Up 2→3, Tone2 Icarus 2→3, Metric Halo ChannelStrip 3→v4 (no free-upgrade language found either way, restored per the "unconfirmed = keep separate" rule). This sweep only catches products with a rename trace in the migration history — see the backlog for the bigger, unbounded "was an older version ever added at all" question. **Sellers can add a missing plugin themselves 2026-09-24** (migration `0035_seller_submitted_plugins.sql`; see Catalogue maintenance rules — strategy discussed with Victor after the FabFilter episode): instead of us pre-guessing every developer's whole product history, the Sell form now lets a seller add their own plugin when it is not in the list — developer still picked from the existing list (that is where the transfer-rules policy lives), plugin name typed freely + a category chosen, `create_listing()` creates the `plugins` row on the fly (`created_by` set to the seller, so these are easy to spot and clean up later; duplicates under the same developer/name reuse the existing row instead of creating a second one). This is the main lever going forward for catalogue completeness — the old "email contact@" hint is gone. **Neural DSP policy text updated 2026-09-24** (migration `0036_neural_dsp_policy_update.sql`, Victor found two forum threads): the official support article cited since 0019 is gone and neither the support site nor the terms of service mention transfers. The only details are user reports on Neural DSP official forum (transfer to another user via Neural DSP support, $25 admin fee per license, free promotional Quad Cortex licenses excluded), so the policy stays unverified (`transferable = null`), but the "What we know" text now carries those details and the dead source link was replaced by the forum thread. Flip to `transferable = true` (with fee/process/who pays) once Neural DSP confirms in writing or publishes a page. **Done 2026-09-25** (migration `0039_neural_dsp_transferable.sql`): Neural DSP Support confirmed by email to Victor that paid perpetual licenses transfer through iLok License Manager (seller pays the iLok fee, $25 per license or bundle, $50 max per transaction, trials/NFR/some promotional licenses excluded), source = the iLok help page they pointed to (help.ilok.com/faq_licenses.html#transfer_between_accounts), now `transferable = true`. **Sign-in and Sell fixed for phone visitors 2026-09-24** (migration `0037_sign_in_redirects.sql` + code; Vercel Analytics: ~80% of visitors come from Instagram/Facebook, 72% on mobile, 41% iOS, and half of /signin visitors never asked for a link; funnel query: 6 links asked, 6 clicked, 6 usernames, 5 Terms, 3 listings, so no email/signup bug): (1) every sign-in entry point (Sell, Buy, Message seller, deal page) now sends the user back where they were after the magic link — the target page is saved server-side by email in `sign_in_redirects` (service role only, read once, 1-hour lifetime) instead of a cookie, because the link usually opens in another browser (Instagram app browser -> mail app -> Safari); users without a username/Terms go through `/account?next=` first. `safeNext`/`signInUrl` live in `src/lib/next-path.ts`. Safe to deploy before the migration: without the table the user just lands on My account as before. (2) The sign-in page no longer says to open the link in the same browser (false since the token_hash template). (3) The Sell form plugin picker is a custom search list instead of a `<datalist>` (which barely works on iPhone): matches every typed word in developer + plugin name, ignores punctuation ("proq" finds Pro-Q), 8 results + a "Not in the list? Add it" option; and the page only sends `id, name, category, developer_id` per plugin, with each developer's rules sent once, instead of the full developer record repeated on all ~3,700 plugins.

**SEO basics + link previews 2026-09-25** (code, no migration; audit found no sitemap/robots, no og:image, and the canonical pointing at the bare domain while the site lives on www): `src/app/sitemap.ts` (static pages, published blog posts, all developer pages, active listings — rendered per request so new listings and scheduled posts appear on their own), `src/app/robots.ts` (keeps /account, /deals, /auth, message threads out), `metadataBase` switched to `https://www.pluginresale.com`. Link-preview images (1200x630, site colours) generated by `src/lib/og.tsx` for home/every page, each listing (plugin, price, transfer badge, Sold/Reserved), each developer page and each blog post; fonts are static TTFs in `assets/fonts/` (ImageResponse cannot use the web fonts). A page that sets its own `openGraph` loses the inherited image, so it needs its own `opengraph-image.tsx` next to it. Search Console: domain property `sc-domain:pluginresale.com`, verified with a TXT record on Namecheap (added by Victor), sitemap submitted as the full URL `https://www.pluginresale.com/sitemap.xml` (a domain property rejects a bare `sitemap.xml`).

**Sonarworks policy verified 2026-09-25** (migration `0039_sonarworks_policy_verified.sql`): `transferable = true` from the EULA (section 4, License Ownership Transfer) and the official help article (seller and buyer each open a support request with both names/emails + activation key, Sonarworks must approve). No fee or delay stated anywhere, left empty. Victor is emailing the other unverified developers as a private customer (from his own address) to get their policies: a reply only counts once it points to an official page we can cite, a support chat or email answer alone is not a source.

**Listing page on phones, Browse polish, listing alerts 2026-09-25** (migration `0038_listing_alerts.sql` + code): on screens under 1000px the listing page shows the price card (Buy / Message seller) right under the plugin name instead of after the transfer-rules box (CSS `order` with the two column wrappers set to `display: contents`); the Browse sort applies on change (`SortSelect`); branded `not-found.tsx`; Sell form description example. **Alerts**: an empty Browse search now lists the matching catalogue plugins (`search_plugins()` RPC, same word rules as the Sell picker) with "Alert me" (signed-in, `listing_alerts` table, owner-only RLS), "I own it, sell it" (`/sell?plugin=<id>` preselects the plugin) and the developer rules; plugins whose developer forbids transfers say so instead. When a listing is created, the Sell action emails every alert holder for that plugin (service role, `after()`, never the seller). Alerts are managed in My account (`#alerts`) and deleted with the account; privacy policy updated (EN + FR). Note: `listing_alerts` cascades on plugin delete, so a catalogue merge silently drops alerts on the removed row — move them first if that ever matters.

**Positive Grid not transferable + header label 2026-09-25** (migration `0041_positive_grid_not_transferable.sql` + code): Positive Grid support confirmed by email (request 599279) that software licenses can never be transferred, backed by its Terms and Conditions section 10, now `transferable = false`. With Neural DSP (0039) the 191-developer study (`plugin-transfer-policies-study-2026.md`, publishes 1 Oct) was refreshed: 107 allowed / 55 not / 29 not verified, recounted from the migrations replayed locally. The header nav item for `/developers` now reads "Transfer rules" (same as the footer and the page title), URL unchanged.

**HOFA transferable 2026-09-25** (migration `0042_hofa_transferable.sql`): confirmed by HOFA support (ticket 27577) and its official resale page, €20 per product (max €50), EDU and partial bundles excluded; study figures refreshed (108 / 55 / 28).

**Share button + price in Google 2026-09-25** (code, no migration): `ShareButton` (`src/components/ShareButton.tsx`) opens the phone share sheet (Web Share API), or copies the link on desktop. On a listing: prominent "Share my listing" for the seller (in the price card, and in the green notice right after publishing), a discreet "Share this listing" link for visitors. The listing page also carries Product JSON-LD (price in EUR, used condition, InStock / OutOfStock when reserved / SoldOut, image = the listing link-preview image) so Google can show the price under the link. Idea parked by Victor for later: make each plugin on a developer page clickable (to its listings, or the alert / sell options).

**Fender transferable 2026-09-25** (migration `0043_fender_transferable.sql`, Victor found the page): the official Fender/PreSonus support article "Fender Software License Transfer" (updated Aug 2026, covers Fender Studio Pro) allows permanent transfers, self-service from my.fender.com (My Gear > Transfer License), seller pays $25 for Studio Pro ($10 Notion/Progression, $5 per add-on); NFR, hardware freebies, Prime/demo/beta and post-subscription perpetual licenses excluded. The web page returns 403 to bots, the text was read through the Zendesk API (`support.presonus.com/api/v2/help_center/en-us/articles/360045106552.json`). DAW article section rewritten (`updated: 2026-09-25`), study figures refreshed (109 / 55 / 27).

## Backlog (not scheduled yet)
- **Backfill older paid-upgrade versions that were never merged, just never added** (raised 2026-09-24, not started): the 2026-09-24 audit (0032-0034) only found and fixed products that show a trace of being wrongly *renamed/merged* in a past migration — a bounded, searchable set. It did not (and, done by hand, realistically cannot in one pass) check every one of the ~3,700 products for whether an older, still-resellable major version exists but was simply never added at all, for every one of the 191 developers. This is a much bigger, open-ended research job (each developer's own back-catalogue and upgrade pricing), only worth doing if it turns out to matter in practice (a seller emailing about a specific missing older version, like the FabFilter case) — tackle it product-by-product as reports come in rather than as one big sweep, unless Victor asks for the full sweep.
- **Retention jobs promised in the Privacy Policy** (none needed before 2027, but must exist in time): delete accounts inactive 3 years (with an email warning first), delete deals/messages/reviews 5 years after they end, delete reports 1 year after handling. `deleted_accounts` is already purged on each new deletion.
- **Before a Terms change**: the Terms promise an email to all users 15 days before any important change.
- **Legal decisions (Victor, 2026-09-24)**: legal pages (`/terms`, `/privacy`, `/legal`) have a French version in a second part of the same page (`#fr`), French prevails if they differ — keep both in sync on every edit. Consumer mediator = CM2C (Victor's existing membership for coolkidsaudio.com; check with CM2C that it covers pluginresale.com), in Terms §19 and on `/legal`. Transfer rules database: "as is", information-only disclaimer (Terms §7, also on the TransferRules box). DAC7 very likely out of scope (licenses are intangible) — to confirm with the accountant (Dougs).
- **Blog: interviews with audio engineers** (Victor, 2026-09-24): real, natural content, good for SEO. Format and who to approach still to define.
- **Make an offer**: let a buyer propose a price below the listing price instead of buying at the listed price outright. Needs design (where it fits in the deal flow, how the seller accepts/declines/counters). Raised by Victor 2026-09-23, explicitly not urgent.

## Supabase setup (done by hand in the dashboard)
- **service_role gets no table privileges automatically on this project** (found 2026-09-25, migration `0040_service_role_grants.sql`): a table the server reads or writes with `SUPABASE_SERVICE_ROLE_KEY` needs an explicit `grant ... to service_role`, otherwise every call fails with "permission denied" (errors are only logged). The Auth admin API (`getUserById`, `deleteUser`) is not affected. 0037 (sign-in return) and 0038 (alert emails) were silently broken until 0040.
- SQL migrations live in `supabase/migrations/` and are pasted into the SQL Editor in order. The Supabase SQL Editor rejects scripts whose `--` comments contain an apostrophe or a semicolon ("syntax error at end of input, LINE 0"): keep comments free of `'` and `;`, or hand Victor a comment-free copy to paste. Catalogue audit 0024-0026 applied 2026-09-24 (comment-free copies), `select count(*) from plugins` = 3717 as expected.
- Auth emails: custom SMTP is live (Resend, domain pluginresale.com verified via DNS — DKIM/SPF/DMARC records on `resend._domainkey` / `rsend` / `send` / `_dmarc`, no impact on the existing MX/SPF for inbound forwarding). Magic Link and Confirm signup templates use `supabase/templates/magic-link.html` (token_hash link, works cross-device/cross-browser). Auth rate limit raised to 30 emails/hour.
- Auth URL config: Site URL `https://www.pluginresale.com`; redirect URLs include production, localhost:3000 and Vercel previews.
- App env vars needed on Vercel (Project Settings → Environment Variables), on top of the two Supabase ones: `SUPABASE_SERVICE_ROLE_KEY` (Supabase → Project Settings → API → service_role secret — server-only, full DB access, never expose to the browser) and `RESEND_API_KEY` (same key as the Auth SMTP password). Both are already in `.env.local` for local dev.

## Backups
- `.github/workflows/db-backup.yml` dumps the database every night (roles, schema, data, auth data) with the Supabase CLI, encrypts it with GPG (AES-256) and stores it as a GitHub Actions artifact kept 30 days. **The repo is public**: artifacts are downloadable by anyone, hence the encryption; never print database content in the workflow logs.
- Repo secrets (GitHub → Settings → Secrets and variables → Actions): `SUPABASE_DB_PASSWORD` = the database password only (Supabase → Database → Settings → Reset database password; the workflow builds the Session pooler URL itself), `BACKUP_PASSPHRASE` = long random passphrase, kept by Victor in his password manager (lost passphrase = unusable backups).
- Restore: download the artifact (Actions → latest "Database backup" run), `gpg -d pluginresale-db-DATE.tar.gz.gpg | tar xz`, then in `backup/` run the psql restore from the Supabase docs (backup-restore guide) against the target project: `psql --single-transaction --variable ON_ERROR_STOP=1 --file roles.sql --file schema.sql --command 'SET session_replication_role = replica' --file auth_data.sql --file data.sql --dbname "<connection string>"` (on a fresh Supabase project, auth tables already exist: empty them first or drop `auth_data.sql` if `data.sql` already contains auth rows).

## Important
- Namecheap DNS already has email forwarding (MX + SPF). When pointing the domain to Vercel, **only add A / CNAME records, never touch MX or the SPF TXT.**

@AGENTS.md
